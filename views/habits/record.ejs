<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f0f2f5;
        margin: 0;
        padding: 20px;
    }

    h1 {
        text-align: center;
        margin-bottom: 30px;
        color: #333;
    }

    .habits-container {
        display: flex;
        flex-direction: column;
        gap: 20px;
        max-width: 600px;
        margin: 0 auto;
    }

    .habit {
      background-color: #fff;
      padding: 20px;
      border-radius: 15px;
    }

    .habit-name {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 15px;
      color: #444;
    }

    .week-days {
      display: flex;
      justify-content: space-between;
    }

    .day {
      width: 35px;
      height: 35px;
      border-radius: 50%;
      background-color: #e0e0e0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      color: #333;
      transition: background-color 0.3s;
    }

    .day:hover {
      background-color: #4caf50;
      color: white;
      cursor: pointer;
    }
    .habit-icon {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      margin-right: 15px;
      vertical-align: middle;
    }
    .habit-header {
  display: flex;
  align-items: center; 
}
    </style>
</head>
<body>
    <h1> my habits</h1>
    <div class="habits-container">
        <% allHabits.forEach((oneHabit) => { %>
            <div class="habit">
                <div class="habit-header">
                    <img src="/images/<%= oneHabit.icon %>" alt="icon" class="habit-icon">
                    <div class="habit-name"><%= oneHabit.name %></div>
                </div>

            </div>
        <% }) %>

</body>
</html>